# INTRODUCTION

# How to use
1. Create a `.env` file in the same directory as `main.py`.
2. Add the line `app_pass="your_app_password"` to the `.env` file, replacing `"your_app_password"` with the actual app password you generated for your email account.
3. Run the `main.py` script to send an email.